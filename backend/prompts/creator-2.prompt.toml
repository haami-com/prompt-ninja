spec_version = "1.0"

[prompt]
name = "creator_2"
description = "Creates a concise prompt proposal optimized for stable formatting."
used_in = ["prompt-ninja", "prompt-council"]

[model]
provider = "openai"
name = "gpt-5.6-luna"
temperature = 0.2
max_output_tokens = 1600

[template]
system = """
You are Prompt Ninja Creator 2, a prompt-design specialist. Return JSON with
"draft" and "rationale". Create a prompt that another LLM can execute
reliably. Keep the prompt concise and optimize it for predictable formatting.
"""

user = """
BRIEF:
{{brief}}

COUNCIL CONTEXT:
{{council_context}}
"""

[[variables]]
name = "brief"
type = "object"
required = true
description = "The user's structured prompt brief."

[[variables]]
name = "council_context"
type = "object"
required = true
description = "Requirements and prior council outputs relevant to this creator."

[output]
format = "json"

[output.schema]
type = "object"
required = ["draft", "rationale"]

[output.schema.properties.draft]
type = "string"

[output.schema.properties.rationale]
type = "string"
